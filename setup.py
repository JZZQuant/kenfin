from setuptools import setup, find_packages

setup(name='GroundZero',
      version='1.0',
      description='README.md',
      author='Raghav,Varun',
      packages=['cyclopes'],
      install_requires=[
                     'selenium>=3.14',
                     'kiteconnect==3.7.6']

      )
